# OfficeCLI Repo Learning Plan (before any further deck work)

**Created:** 2026-09-02 (operator-directed: STOP building, plan how to read/understand/learn/review the repo first)
**Trigger for execution:** next session, BEFORE touching PROPOSAL_DECK* files again — **SATISFIED 2026-09-02 (SM53): Phases A-C executed in full with written takeaways; Phase D1 (operator style pick + present-vs-rebuild decision) is the remaining gate. No deck edits were made this session.**
**Why:** three deck iterations were built on partial tool knowledge. The repo (1,338 files) was never systematically explored: `skills/` (186 files, incl. 25 designed reference styles), full SKILL.md, docs, plugins, schemas — all unread. Candidate C failed because I refilled a morph-ppt style deck without reading how that system works.
**Rule:** each item gets a written takeaway in this file before the next item starts. No deck edits until all items are done.

---

## Phase A - Read

- [x] **A1. SKILL.md (repo root) - FULL read.** DONE 2026-09-02 (SM53; repo cloned shallow to `research/code/OfficeCLI/`, 168 MB, 1,218 files). Takeaways: (1) skill-loading rule - most specific match wins, ONE skill per artifact, never stack; **morph-ppt routes STATIC decks to `pptx`** - our deck class is `pptx`, not morph; (2) `--best-effort` flag on batch restores apply-what-succeeds for lossy dump-replays (atomic rollback was biting us - relevant to refill pipeline); (3) `view screenshot --grid N` renders all slides as one PNG grid (B1 accelerator); (4) `goto` scrolls watch browsers to an element; (5) help system is schema-authoritative (`help pptx <element>`), format aliases word/excel/ppt.
- [x] **A2. skills/ inventory + morph-ppt + pptx base skill docs.** DONE 2026-09-02. pptx v2 base skill (568 lines) = the actual rulebook for our static deck class: title >=36pt bold / body >=18pt / title >= 2x body; explicit sizes on every shape (never theme-inherit); 12-col grid on 33.87x19.05cm, margin >=1.27cm, gap >=0.76cm (pick ONE gap value), >=20% negative space, card-width formula; font pairing table (8 seeds; Consolas+Calibri = developer/tools); 10 canonical palettes w/ roles (primary 60-70% / secondary / accent / text / muted); chart-choice table (KPI = large-text shape NOT chart; category = column; 2-5 slice pie only); layout-pattern table; motif commitment (pick ONE, declare in build plan); **AI-tells to avoid: no decorative title underline, no rounded card + colored left-border stripe, no emoji icons**; title-sequence-first workflow (write all titles, check the arc reads, THEN build); Delivery Gates 1/2/2b/3 (Gate 3 = per-slide screenshot audit vs 12-item adversarial checklist, fix-verify max 3 cycles, then save); build recipes with real coordinate math (KPI card grid 9.78cm cols, flowchart gap math). INDEX.md: 52 styles w/ bg/fg/accent hex + mood + use-case lookup ("Developer/Technical: cyber-future, blueprint-grid, tech-cosmos"; "Pitch: investor-pitch, premium-navy, project-proposal, glassmorphism-vc, obsidian-amber"). 27 styles ship prebuilt demo .pptx; INDEX repeats the do-not-copy-coordinates warning.
- [x] **A3. examples/README.md + plugins/plugin-protocol.md + schemas/README.md.** DONE 2026-09-02. Takeaways: (1) examples ship as 4-file sets (.md walkthrough + .sh CLI build + .py SDK build + prebuilt output) - .sh/.py are equivalent twins; notable for future units: `ppt/diagram.sh` renders **Mermaid to native editable shapes** or PNG, per-type chart/table/transition scripts, `tables-financial` end-to-end deck; (2) plugins v1: 3 kinds (dump-reader migrates foreign->native w/ sibling cache; exporter = one-shot native->foreign, source read-only; format-handler = session-long IPC); PDF export = exporter plugin (not installed - matches SSOT); discovery: env var -> ~/.officecli/plugins -> bundled -> PATH; (3) schemas/ = capability SSOT embedded in the binary at build time + CI contract tests - confirms `help` output is authoritative, skill docs can drift.

## Phase B - Look (build visual judgment)

- [x] **B1. Render + VIEW the reference styles.** DONE 2026-09-02 (SM53): all 27 prebuilt demo decks rendered to `style_gallery/*.png` via `view screenshot --grid 3` and individually vision-inspected (verdict + readability + fit). Gallery lives at `project/proposal/style_gallery/` for the operator. Summary of verdicts (fit as-is for a serious grad-CS proposal): best 3/5 = bw--mono-line (Swiss minimal, "academic reports" register), dark--editorial-story (white/slate/red editorial rigor, strongest projection contrast), dark--spotlight-stage (black/white/gold keynote, high contrast). 2.5 = dark--cyber-future (AI/security thematic, cyan-on-dark), dark--investor-pitch, light--bold-type. Everything else 1.5-2 (warm/vivid/playful families: wrong register). CROSS-CUTTING FINDING: every demo's body text is small/low-contrast - confirming the styles are demos, not refill templates; per pptx v2 our build owns sizing (>=36/18pt + contrast floor), so readability is supplied by methodology, not inherited from the demo. Caveat recorded: vision verdicts judge demo CONTENT too; only palette/typography/motif transfer.
- [x] **B2. Shortlist top 3 for QueryGuard.** Criteria: security/tech tone, projection readability, proposal-structure fit. **(1) dark--editorial-story** `FFFFFF/2C3E50/E74C3C` - editorial-academic register, best contrast, serif-capable. **(2) bw--mono-line** `FFFFFF/1A1A1A/C8C8C8` - calm Swiss restraint, INDEX-named for academic reports; risk: too airy (needs denser evidence layouts). **(3) dark--cyber-future** `0B0C10/66FCF1/1F2833` - thematic AI-security fit, INDEX Developer/Technical; needs our contrast discipline (their gray-on-dark fails projection). Honorable mention: **light--project-proposal** `E8EEF4/1E3A5F/D4A84B` - literally named for project proposals, "rigorous" mood, but ships style.md only (no demo deck); equally usable since the corrected workflow borrows palette+motif, not slides.

## Phase C - Understand the style machinery

- [x] **C1. Read the chosen style's build.sh + style.md + assets.** DONE 2026-09-02 (blueprint-grid, the candidate-C style). Machinery understood: every shape is a morph scene actor (`!!grid-h1`, `!!dot1`, `#s1-title` prefixes); grid lines/dots/rings are individually positioned shapes with hand-tuned cm coordinates; demo text ("AI Agent Platform", Courier New 56pt) has boxes sized to ITS length (width=24cm etc.). The style is a scripted morph demo, not a parameterized template - there is no "fill in your content here" mechanism; refilling means fighting coordinates tuned for different content.
- [x] **C2. Diagnose candidate C against intended usage.** DONE 2026-09-02 - rendered `style_gallery/CANDIDATE-C-blueprint-refill.png` and vision-diagnosed. Named breakages: uneven canvas (5 slides ragged 3+2 in grid shot), gold markers lost/replaced by low-contrast blue dots, grid lines competing with text (no hierarchy separation), cramped 3-pillar columns with clipped right edge, cards misaligned with the grid, blue-on-blue "60"/"<50ms" contrast failures, tiny unreadable labels/footer, inconsistent typography scale. Root cause (one line): text dropped in without rebalancing scale/contrast/spacing/grid alignment - exactly the vendor's documented INDEX warning. **Corroboration of a known blind spot: candidate C PASSED `view issues` (0 findings) while carrying all of this - the issues auditor checks mechanical overflow/contrast-on-same-fill, not composition; the pptx-v2 Gate 3 screenshot audit is the design-level check that was never run.**
## Phase D - Review and rebuild (CORRECTED 2026-09-02 after Phase A-C evidence)

**Method correction (vendor-docs-grounded):** the original D2 "rebuild using the style's own build mechanism" is wrong - build.sh regenerates the vendor's demo, coordinates tuned to demo content; there is no content-slot mechanism. Corrected workflow per morph-ppt SKILL.md + pptx v2: **load skill `pptx`; build the deck from scratch on the 12-col grid with the visual floor (>=36/18pt, contrast, >=20% negative space, motif commitment); borrow ONLY palette + signature gesture + typography feel from the operator-picked style; verify with Delivery Gates 1/2/2b + Gate 3 screenshot audit (the design-level check).**

- [x] **D1. Operator decision RECEIVED 2026-09-02: present with candidate A tomorrow (Sep 3); the from-scratch rebuild in a picked style becomes the Phase 2+ standard (style pick deferred to that unit; shortlist stands).** Gate-3 screenshot audit on candidate A added as tonight's closeout (the check candidate C never had).
- [ ] **D2. Rebuild per the corrected workflow above (operator-gated).**
- [ ] **D3. Full gates 1/2/2b + Gate 3 per-slide screenshot audit (12-item checklist) + operator `watch` design pass.**

## Facts already recorded (do not re-derive)

- GLM Slide Agent: PDF-only export; API not exposed (key = base models only); operator skipping. Monitored in SSOT.
- OfficeCLI Discord: dead (single bug-report channel). Do not suggest as support/community channel.
- AI template collections (ai-ppt-template/free-ppt-template, 595 decks): picture-flattened, unusable for content-fill.
- Resident write-trap: close the officecli resident BEFORE external disk edits, or idle auto-flush overwrites them.
- Vendor repo `examples/ppt/templates/styles/` + `skills/morph-ppt/reference/styles/` = 14 buildable style generators (examples) + 52-style library of which **27 ship prebuilt demo .pptx** (corrected count 2026-09-02 from the fresh clone; the "25" was the download-day count). Styles are DESIGN REFERENCES, not refill templates - see Phase A root-cause finding.
